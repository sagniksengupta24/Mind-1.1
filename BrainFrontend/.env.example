# Server-side Backend URL where the Mind1.1 API is running
MIND_BACKEND_URL=http://localhost:8765

# Optional: Server-side API token for authenticated requests (must match MIND_API_TOKEN on the backend)
MIND_API_TOKEN=
